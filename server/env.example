# Copy this file to .env and update the values

# MongoDB Atlas Configuration
MONGODB_URI=mongodb+srv://loveohara:l07WI2DtfaZYyLrm@cluster0.fgmlgyv.mongodb.net/handshake?retryWrites=true&w=majority&appName=Cluster0

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret (change this in production)
JWT_SECRET=your_super_secret_jwt_key_change_in_production

# Session Secret
SESSION_SECRET=your_session_secret_key

# Instructions:
# 1. Copy this file to .env in the same directory
# 2. Make sure your IP is whitelisted in MongoDB Atlas
# 3. Update the MONGODB_URI if you have different credentials 